# 独立音频环境（.venv-audio）依赖
# 说明：
# - 主流程环境不再安装这些重依赖，避免与 pydantic-ai / provider 生态冲突。
# - Torch CUDA 版本在 setup_audio_venv.ps1 里单独安装并锁定。

python-dotenv>=1.2.1
httpx>=0.27.0
numpy>=2.4.1
scipy>=1.17.1
huggingface-hub>=1.4.1
audio-separator>=0.44.0
onnxruntime-gpu>=1.24.0
