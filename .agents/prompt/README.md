# Prompt Library

This directory is the repository-versioned prompt snippet library used by
`StyleContext`. Keep reusable visual prompt fragments here instead of creating
rotating local prompt roots.

Claude-style Skills stay in `.agents/skills/`; this directory only stores
runtime-loadable prompt references.
